import React from 'react'
import ReactDOM from 'react-dom'

const Option: React.FC<{}> = () => {
  return null
}

const root = document.createElement('div')
document.body.appendChild(root)
ReactDOM.render(<Option />, root)
